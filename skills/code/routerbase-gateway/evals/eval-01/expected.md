The answer should provide a Python OpenAI-compatible client using `base_url="https://routerbase.com/v1"` and `api_key=os.environ["ROUTERBASE_API_KEY"]`. It should use a RouterBase example model ID, state that the model should be verified against current availability, and include a brief checklist covering server-side environment variables, smoke testing, and log hygiene. It must not include a real API key or suggest putting credentials in client code.
