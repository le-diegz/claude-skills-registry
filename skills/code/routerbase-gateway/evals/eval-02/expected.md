The answer should explain that image and video workflows use different endpoint families and may have different sync behavior. It should route image generation to `/v1/images/generations`, video generation to `/v1/videos/generations`, and describe asynchronous video handling with task IDs, polling with backoff, optional callbacks, result persistence, and safe logging. It should avoid promising permanent media retention or assuming every media model supports the same parameters.
